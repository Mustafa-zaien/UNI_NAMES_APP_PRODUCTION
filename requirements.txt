# Core runtime dependencies for UNI_NAMES_APP_PRODUCTION
# Pinned to safe, compatible ranges for Python >=3.9

# GUI
PyQt6>=6.6,<7
qtawesome>=1.3,<2

# Data & Excel
pandas>=2.1,<3
numpy>=1.26,<3
openpyxl>=3.1,<4

# ML / Similarity
scikit-learn>=1.4,<2
rapidfuzz>=3.0,<4

# (Optional) For building EXE locally only
# pyinstaller>=6.3,<7
